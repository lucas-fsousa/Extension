namespace PublicUtility.Extension {
  public enum SearchType {
    Symbols,
    Numbers,
    Letters,
    NumbersAndLetters,
    SpecialChars,
    UpperCase,
    LowerCase,
    WhiteSpaces
  }

  public enum TokenType {
    Bearer,
    Basic
  }

  public enum ConvertTo {
    Number,
    Char,
    String,
    Bool
  }

}
